$schema: "https://json-schema.org/draft/2020-12/schema"
allOf:
  - $ref: "./resp.success.yaml"
  - type: object
    properties:
      data:
        type: object
        propertyNames:
          pattern: "^[A-Z]{3}$"
        additionalProperties:
          type: object
          propertyNames:
            pattern: ^[A-Z]{3}$
          additionalProperties:
            type: number
