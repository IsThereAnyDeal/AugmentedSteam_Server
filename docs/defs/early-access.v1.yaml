description: Success
content:
  application/json:
    schema:
      type: array
      items:
        type: integer
    examples:
      success:
        value:
          $ref: "../examples/early-access.v1.success.json"
