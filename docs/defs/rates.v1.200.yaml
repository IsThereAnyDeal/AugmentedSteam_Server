description: Success
content:
  application/json:
    schema:
      type: object
      propertyNames:
        pattern: "^[A-Z]{3}$"
      additionalProperties:
        type: object
        propertyNames:
          pattern: ^[A-Z]{3}$
        additionalProperties:
          type: number
    examples:
      success:
        summary: "Rates response example"
        value:
          $ref: "../examples/rates.v1.success.json"
