description: Success
content:
  application/json:
    schema:
      type: object
      propertyNames:
        type: string
      additionalProperties:
        type: object
        required: [img, url, price]
        properties:
          img:
            type: string
          url:
            type: string
          price:
            type: number
    examples:
      success:
        value:
          $ref: "../examples/market.cards.v2.success.json"
