description: Success
content:
  application/json:
    schema:
      type: array
      items:
        type: array
        minItems: 2
        maxItems: 2
        items:
          type: string
    examples:
      success:
        value:
          $ref: "../examples/profile.background.list.v2.success.json"
